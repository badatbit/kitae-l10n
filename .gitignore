# Extracted game data and build output stay out of the repo: it is the
# publisher's content, and the disc images run to ~1.1 GB each.
# Everything under these directories is reproducible from your own disc dump
# with the tools in tools/.
dump/
dist/

# stray local files
*.pyc
__pycache__/
*.bin
*.gdi
*.raw
*.CB
*.DLL
*.EXE
