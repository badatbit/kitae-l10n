work/
dist/
dump/
*.pyc
__pycache__/
*.bin
*.gdi
*.raw
*.CB
*.DLL
*.EXE
tmp/

# 빌드 아티팩트·편집 백업
*.egg-info/
*.bak
*.bak[0-9]
*.bak_*
_textbak/
# 합성 이미지 캐시 — erased+원장에서 compose 로 재생성된다
images/injected/

# 조사용 스크래치 덤프
/scratch_targets.txt

# kitae 쪽 이미지 작업판 — 자국(jaguk)은 originals/erased 만 읽고 여기는 안 본다.
# 변형본(.notext·.ko·후보)이 쌓이는 곳이라 저장소에 두지 않는다. 원본 덤프에서 다시 만든다.
images/base/
