# 北へ。White Illumination — 한국어화

드림캐스트 『北へ。White Illumination』(허드슨, 1999)의 한국어 팬 번역.
사용자 본인이 뜬 디스크 덤프, 개인 목적. 패키지 `kitae`, 빌드는 `python -m kitae build`.

이 문서는 **번역할 때 반드시 지킬 규칙**을 모은다. 여러 에이전트가 같은 규칙으로
번역하도록, 규칙마다 "왜"를 붙였다 — 근거를 알아야 흔들리지 않는다.

---

## 번역 규칙 (어기면 화면이 깨지거나 싱크가 틀어진다)

### 1. ★ 전부 전각(全角)으로 쓴다 — 가장 중요

대사문의 **모든 글자는 전각(2바이트)**이어야 한다. 반각(1바이트)을 섞으면 음성
싱크가 깨진다.

- 숫자: `０１２３４５６７８９` (반각 `0-9` 금지)
- 영문: `ＡＢＣ…ａｂｃ…` (반각 `ABC` 금지)
- 기호: `。、！？・（）「」『』：／＋－％～` (반각 `.,!?()` 금지)
- 어절 사이 공백: **전각 공백 `　`(U+3000)**. 일반 스페이스 금지.

**왜:** MTG 타이밍(`docs/MTG-TIMING.md`)은 엔진이 소비하는 항목 수가
`M = 줄수 + 2바이트문자수`다. 반각은 항목을 안 만든다. 싱크 조건이
`반각수 H ≤ 줄수 L − 1`이라 **한 줄 창은 반각을 하나도 못 쓴다.** 전각만 쓰면
`M = 줄수 + 글자수`로 원문과 똑같이 동작한다. 반각을 쓰면 문장 뒷부분 글자가
프레임당 하나씩 쏟아진다.

### 2. 한 줄 25글자 상한

한 줄에 그려지는 글자는 **최대 25개**(전각 공백 포함). 넘으면 토큰화는 되지만
26번째부터 **화면에 안 그려진다**.

**왜:** 렌더러의 레코드 배열이 `obj+0x84`~`obj+0x213` = 25×16B로 고정
(`docs/MTG-TIMING.md`). 폭과 무관한 글자 수 제한이다.

### 3. 줄 구조를 1:1로 보존한다

원문의 `(window, line)` 하나가 번역 하나에 대응한다. **줄을 합치거나 나누지
않는다.** 원문이 2줄이면 번역도 2줄, 각 줄을 제자리에 번역한다.

**왜:** 빌드가 줄 경계 시각을 원본에서 물려받는다(`runner.py::_timing_specs`).
줄 수가 다르면 그 대응이 깨진다. 번역은 줄 단위로 관리한다.

### 4. 번역량을 늘리지 않는다

원문에 있는 만큼만 옮긴다. 설명을 덧붙이거나 문장을 쪼개 늘리지 않는다. 창·줄
구조를 그대로 두고 그 안에서 자연스럽게 줄인다.

### 5. ★ 음성 쉼 자리에 문장부호를 둔다 (기호 단위 싱크)

원문 음성은 문장이 끊기는 자리(문장부호)에서 쉰다. 빌드가 타이밍을 리샘플할 때
**쉼을 문장부호(`。、！？・・…`) 뒤로 당겨** 맞춘다(`docs/MTG-TIMING.md` `_snap`).
그러려면 번역문이 **원문이 쉬는 그 자리에 문장부호를 가지고 있어야** 한다.

- 원문의 `、`·`。`·`・・` 가 있던 의미 단락을 번역에서도 **같은 자리에 남긴다.**
  원문 `応。　でも・・` → `응。　근데・・` (쉼표·말줄임 위치 유지)
- 원문에 쉼표가 있는데 번역에서 없애면, 성우가 쉬는 순간 낱말 한가운데서 멈춘다
  (`응. 아〈쉼〉침`). 반대로 없던 자리에 넣지도 않는다.
- 문장부호 개수·종류를 원문과 억지로 맞출 필요는 없지만, **큰 쉼(`・・`, `。`)의
  위치는 원문을 따른다.** 이게 "기호 단위로 음성을 맞추는" 규칙이다.

### 6. 마크업은 그대로 둔다

`@S@` `@P@` 같은 `@…@`, `&主人公&` 같은 `&…&`는 제어 코드다. **번역하지 말고
위치도 유지**한다. 글자 수에는 안 들어간다(정규식 `@[^@]{0,8}@|&[^&]{0,16}&`로
빼고 센다).

---

## 음차·표기 규칙 (일→한)

`docs`와 `translation/glossary.json`에 근거. 요지:

- **어두 거센소리 유지**: か=카, た=타, ぱ=파 (어두에서 예사소리로 바꾸지 않는다)
- **つ = 츠** (쓰 아님)
- **탁음만 예사소리로**: が=가, だ=다, ば=바
- **고유명은 음차, 기관·일반명사는 번역**:
  - `中央` → 추오 (지명일 때), 그러나 기관어는 뜻으로
  - `信金`/`信組` → 신협
  - 예: `北海道庁` → `홋카이도　도청` (도청은 번역)
- 장음은 한글에 반영하지 않는다(음차 관행).

### 확정 표기 (glossary.json — `kitae check`가 강제)

| 원문 | 번역 | 이유 |
|---|---|---|
| `北海大学` | `홋카이도　대학` | 실제 홋카이도 대학. 자리 넉넉해 풀어 씀 |
| `北海大` | `홋카이대` | 원문이 `大学`/`大`를 갈라 써서 한국어도 갈라 씀 |
| `北海道庁` | `홋카이도　도청` | `홋카이도청`은 경계가 안 보임 |
| `北海健` | `홋카이켄` | 가공 상호라 음차. `北海道`로 오독 주의 |

새 고유명을 확정하면 `glossary.json`에 `why`와 함께 추가한다.

---

## 파일 형식

번역 원본은 `translation/*.json`. 스키마(대사):

```json
{
  "script": "<원본 스크립트명>",
  "source": "ja",
  "entries": [
    {
      "window": 0, "line": 0,      // 창·줄 번호 (건드리지 않는다)
      "speaker": "...", "kind": "...", "scene": 0,
      "date": "08-04",              // 이야기 진행 날짜 (MM-DD)
      "slot": "...", "place": "...", "voice": "...",
      "chars": 12,                  // 참고용 글자 수 (빌드가 ko에서 다시 셈)
      "text": {
        "ja": "원문",
        "ko": "번역 — 없거나 ja와 같으면 미번역"
      }
    }
  ]
}
```

- **`text.ko`만 채운다.** 다른 필드는 손대지 않는다.
- `chars`는 참고용 — 빌드가 실제로는 `ko`의 글자 수를 다시 센다. 굳이 갱신
  안 해도 되지만, 25 상한 확인에 쓰면 좋다.
- 미번역 판정: `ko`가 비었거나 `ja`와 동일.
- 날짜별 작업: `date` 필드로 거른다(예 `08-04`). 한 날짜가 여러 히로인 파일에
  흩어져 있다(KAORU·KOZUE·MEGUMI·TANYA·風呂 등).

---

## 검증

```
python -m kitae check     # glossary 위반 등 정적 검사
python -m kitae build     # 폰트 주입·타이밍 리샘플·디스크 패치까지
```

빌드는 번역 글자 수가 바뀐 창의 MTG 타이밍을 새 길이로 자동 리샘플한다
(쉼을 문장부호 뒤로 맞춘다). 번역자는 타이밍을 손대지 않는다.

### 셀프 체크 (번역 후)

- [ ] 반각 문자·일반 스페이스가 없는가 (숫자·영문·기호·공백 전부 전각?)
- [ ] 각 줄 25글자 이하인가
- [ ] 줄 수가 원문과 같은가 (합치거나 나누지 않았나)
- [ ] 마크업 `@…@`·`&…&`을 그대로 뒀는가
- [ ] glossary 표기를 지켰는가

---

## 함정

- **cp932 왕복**: 폰트 셀 `0xEEED`/`0xEEEE`는 cp932로 디코딩이 안 된다. 한글은
  자유 셀에 주입되며 코드페이지는 `data/codepage.json`.
- **전각 공백을 빼먹기 쉽다**: 어절을 붙여 쓰면 안 되고 `　`로 띄운다. 이게 곧
  가독성이자 원문의 리듬이다.
- **경로**: 저장소는 `F:\dev-kitahe\kitae-l10n`. (예전 문서·메모리의
  `kitahe-l10n`은 옛 이름이다.)

세부 기술 근거는 `docs/`(특히 `MTG-TIMING.md`, `SCRIPT-SYSTEM.md`,
`PROPORTIONAL-WIDTH.md`)를 본다.
